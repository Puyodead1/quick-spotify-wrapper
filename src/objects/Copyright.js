class Copyright {
  /**
   * creates a new copyright
   * @constructor
   * @param {Object} object
   */
  constructor(object) {
    this.text = object.text;
    this.type = object.type;
  }
}

module.exports = Copyright;
